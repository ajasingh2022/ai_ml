package com.capgemini.sesp.ast.android.ui.service;

import android.app.IntentService;
import android.content.Intent;

/**
 * This intent service would seamlessly handle workorder finish process.
 * To prevent ANR and user dissatisfaction due to slowness coming due 
 * to heavy lifting work in json parsing and to ignore any orientation changes relate side effects
 * this intent service is created 
 * 
 * @since 18th February, 2015
 * @author nirmchak 
 */

public class StandardWorkorderSaveIntentService extends IntentService {


	public StandardWorkorderSaveIntentService() {
		super("StandardWorkorderSaveIntentService");
	}

	@Override
	protected void onHandleIntent(final Intent intent) {
		if (intent != null) {
			final String action = intent.getAction();
			//if (ACTION_FOO.equals(action)) {
				
		}
	}
}
