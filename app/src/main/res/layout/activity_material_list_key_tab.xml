<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/keysHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/heading3"
        android:weightSum="2"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/keyHeader1"
            android:layout_width="0dip"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:gravity="left|center_vertical"
            android:paddingLeft="5dip"
            android:singleLine="true"
            android:textColor="@color/colorBlack"
            android:textSize="@dimen/standard_text"
            android:text="@string/key_no"
            />

        <!-- android:text="Device ID" -->

        <TextView
            android:id="@+id/keyHeader2"
            android:layout_width="0dip"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:textColor="@color/colorBlack"
            android:gravity="left|center_vertical"
            android:paddingLeft="5dip"
            android:singleLine="true"
            android:textSize="@dimen/standard_text"
            android:text="@string/key_info"
            />

    </LinearLayout>
    <ListView
        android:id="@+id/keys_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="#00000000"
        android:layout_below="@+id/keysHeader"
        android:dividerHeight="1dip"
        android:drawSelectorOnTop="true"
        android:fadingEdge="none"
        android:listSelector="@drawable/selector" >
    </ListView>

</LinearLayout>
